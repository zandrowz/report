{% include 'incl/header.html.twig' %}
<title>Deck</title>
<body>
    {% include 'incl/nav.html.twig' %}

    <div class="content-cards">

        <h1>Draw</h1>
<p>One card has been drawn.</p>


        <div class="card">
            <div class="cards">
                {{ card.getCard() | raw}}
            </div>
        </div>
{# <p>Cards left in deck: {{ card[1] }} </p> #}
<p>Cards left in deck: {{ deck|length }}</p>
        <div class="card">
            {% for card in deck %}
            <div class="cards">
            <span class="symbol">
                {{ card.getCard() | raw}}
            </span>
            </div>
            {% endfor %}
        </div>
    </div>

{% include 'incl/footer.html.twig' %}

</body>
</html>
